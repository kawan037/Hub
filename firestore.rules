rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Fully permissive rules to ensure 100% successful operations for all features,
    // including comment deletion, application approval/rejection, and admin/guest interactions.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
